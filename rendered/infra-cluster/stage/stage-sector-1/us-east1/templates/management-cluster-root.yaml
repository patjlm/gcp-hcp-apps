apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: management-cluster-root
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          gcp-hcp/cluster-type: management-cluster
      values:
        environment: stage
        region: us-east1
        sector: stage-sector-1
  syncPolicy:
    preserveResourcesOnDeletion: false
  template:
    metadata:
      labels:
        cluster: '{{.name}}'
        cluster-type: management-cluster
        environment: stage
        region: us-east1
        sector: stage-sector-1
      name: mgmt-apps-{{.name}}
    spec:
      project: default
      source:
        helm:
          valuesObject:
            cluster:
              environment: stage
              name: ''
              projectId: '{{"{{"}} index .metadata.annotations "gcp-hcp/project-id" {{"}}"}}'
              region: us-east1
              sector: stage-sector-1
              vpcId: '{{"{{"}} index .metadata.annotations "gcp-hcp/vpc-id" {{"}}"}}'
        path: rendered/management-cluster/stage/stage-sector-1/us-east1
        repoURL: https://github.com/company/gcp-hcp-apps
        targetRevision: HEAD
      destination:
        namespace: argocd
        server: '{{.server}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=false
