apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infra-cluster-root
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          gcp-hcp/cluster-type: infra-cluster
      values:
        environment: integration
        region: europe-west1
        sector: int-sector-1
  syncPolicy:
    preserveResourcesOnDeletion: false
  template:
    metadata:
      labels:
        cluster: '{{.name}}'
        cluster-type: infra-cluster
        environment: integration
        region: europe-west1
        sector: int-sector-1
      name: infra-apps-{{.name}}
    spec:
      project: default
      source:
        helm:
          valuesObject:
            cluster:
              environment: integration
              projectId: '{{"{{"}} index .metadata.annotations "gcp-hcp/project-id" {{"}}"}}'
              region: europe-west1
              sector: int-sector-1
              vpcId: '{{"{{"}} index .metadata.annotations "gcp-hcp/vpc-id" {{"}}"}}'
        path: rendered/infra-cluster/integration/int-sector-1/europe-west1
        repoURL: https://github.com/company/gcp-hcp-apps
        targetRevision: HEAD
      destination:
        namespace: argocd
        server: '{{.server}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
