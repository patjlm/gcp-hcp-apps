apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infra-cluster-root
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          gcp-hcp/cluster-type: infra-cluster
      values:
        environment: production
        region: us-east1
        sector: prod-canary
  syncPolicy:
    preserveResourcesOnDeletion: false
  template:
    metadata:
      labels:
        cluster: '{{.name}}'
        cluster-type: infra-cluster
        environment: production
        region: us-east1
        sector: prod-canary
      name: infra-apps-{{.name}}
    spec:
      project: default
      source:
        helm:
          valuesObject:
            cluster:
              environment: production
              projectId: '{{"{{"}} index .metadata.annotations "gcp-hcp/project-id" {{"}}"}}'
              region: us-east1
              sector: prod-canary
              vpcId: '{{"{{"}} index .metadata.annotations "gcp-hcp/vpc-id" {{"}}"}}'
        path: rendered/infra-cluster/production/prod-canary/us-east1
        repoURL: https://github.com/company/gcp-hcp-apps
        targetRevision: HEAD
      destination:
        namespace: argocd
        server: '{{.server}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
