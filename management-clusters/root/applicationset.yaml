apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: management-cluster-apps
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/patjlm/gcp-hcp-apps.git
      revision: HEAD
      directories:
      - path: management-clusters/*
      - path: management-clusters/root
        exclude: true
  template:
    metadata:
      name: '{{path.basename}}'
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/patjlm/gcp-hcp-apps.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: argocd
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
